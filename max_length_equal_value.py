#!/usr/bin/env python
"""
 write a function `def maxLength(array_of_integers, value):` to return the max
 size of a subarray of `array_of_integers` to have the sum equal to `value`.
"""

array_of_integers = [x for x in range(100)]
value = 42

def maxLength(array_of_integers, value):
    

if __name__ == "__main__":
    print maxLength(array_of_integers, value)